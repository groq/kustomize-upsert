version: 2
release:
  mode: keep-existing
builds:
  - main: ./
    binary: kustomize-upsert
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
universal_binaries:
  - replace: true
